# agent-factory-assets-public
Public asset host for agent-factory — preview thumbnails (committed) + ephemeral model-file releases that are deleted right after marketplace ingestion. No persistent model files.
